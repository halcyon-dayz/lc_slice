export type AddDataPayload = {
    arrayIndex: number,
    data: any
}

export type AddDataAtIndexPayload = {
    arrayIndex: number, 
    index: number,
    data: any
}


export type RemoveIndexAtPayload = {
    arrayIndex: number, 
    index: number,
}
